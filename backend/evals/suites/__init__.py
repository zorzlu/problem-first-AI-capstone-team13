"""Per-iteration eval suite definitions.

Each suite specifies:
- Task: what capability is being evaluated
- Evaluators: deterministic + LLM judges
- Pass criteria: threshold values for pass/fail gates
- Metrics: values computed and reported in eval results
"""
